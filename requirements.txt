# The core HDL/result scripts use only the Python standard library.
# matplotlib is optional and only needed for scripts/plot_results.py.
matplotlib

# Paper preview generation and visual verification.
reportlab
Pillow
PyMuPDF
